
  # Dynamic Submission Reports Tracker

  This is a code bundle for Dynamic Submission Reports Tracker. The original project is available at https://www.figma.com/design/8USNOeIzlCtGFRTv4AxYIm/Dynamic-Submission-Reports-Tracker.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  